box: golang:latest
build:
  steps:
  - arjen/hugo-build@1.17.0:
        version: "0.21.0"
        theme: gohugo-amp
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: debacontudo.com.br
        basedir: public
