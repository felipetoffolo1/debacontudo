<figure class="op-interactive">
  <iframe class="column-width giphy-embed" src="{{ .Get "src" }}"
  width="{{ .Get "width" }}"
  height="{{ .Get "height" }}" frameBorder="0" allowFullScreen></iframe>
</figure>

